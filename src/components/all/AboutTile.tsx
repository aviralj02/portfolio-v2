import React from "react";

type Props = {};

const AboutTile = (props: Props) => {
  return (
    <div className="relative flex flex-col col-span-2 row-span-2 bg-card rounded-3xl w-full h-full"></div>
  );
};

export default AboutTile;
